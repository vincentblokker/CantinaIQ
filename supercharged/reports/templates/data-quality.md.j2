# Data Quality Report

**Run:** `{{ run.run_id }}`
**Pipeline duration:** {{ (run.finished_at - run.started_at).total_seconds() | int }}s

## Drop cascade

| Stage | Pre | Post | Removed | % | Top reason |
|---|---:|---:|---:|---:|---|
{% for row in drop_cascade %}
| {{ row.stage }} | {{ row.pre_rows | thousands }} | {{ row.post_rows | thousands }} | {{ row.removed | thousands }} | {{ row.pct | pct }} | {{ row.top_reason }} ({{ row.top_reason_count | thousands }}) |
{% endfor %}

## Stage details

{% for stage_name, stage in run.stages.items() %}
### {{ stage_name }}

- **Pre rows:** {{ stage.pre_rows | thousands }}
- **Post rows:** {{ stage.post_rows | thousands }}
{% if stage.drops %}
- **Drops:**
{% for reason, count in stage.drops.items() %}
  - {{ reason }}: {{ count | thousands }}
{% endfor %}
{% endif %}
{% if stage.error %}
- **Error:** `{{ stage.error.type }}` — {{ stage.error.message }}
{% endif %}

{% endfor %}

## Producer-extraction recall

See `reports/generated/producer-extraction-eval.json` (refreshed by
`cantinaiq evaluate producer-extraction`). Headline recall numbers from
the latest run should be referenced in the methodology — this file is
the per-stage drop ledger.

## Cross-references

- `reports/generated/bias-report.md` — Vivino vs. ICE NL import baseline (run via `cantinaiq bias`).
- `reports/generated/bootstrap-ci.md` — bootstrap rank CIs on top producers (run via `cantinaiq bootstrap`).
- `reports/generated/sensitivity.md` — sensitivity sweep on scoring params (run via `cantinaiq sensitivity`).

---

*{{ metadata_footer }}*
